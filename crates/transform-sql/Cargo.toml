[package]
name = "faucet-transform-sql"
version = "1.3.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
documentation = "https://docs.rs/faucet-transform-sql"
categories.workspace = true
keywords = ["sql", "duckdb", "transform", "pipeline", "etl"]
description = "SQL-as-transform for faucet-stream — run DuckDB SQL over each pipeline page (the `batch` relation)."

[dependencies]
# `arrow` so the SQL stage can expose its Arrow `RecordBatch` form
# (via `into_columnar_stage`) — transform-sql is Arrow-native anyway.
faucet-core = { workspace = true, features = ["arrow"] }
duckdb = { workspace = true, features = ["bundled", "vtab-arrow"] }
arrow.workspace = true
arrow-json.workspace = true
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
schemars.workspace = true
tracing.workspace = true
# HTTP-sourced reference relations (#558): fetched once at compile time via a
# blocking client on a dedicated thread. `blocking` is enabled on top of the
# workspace features so the sync compile path can drive it without a runtime.
reqwest = { workspace = true, features = ["blocking"] }

[dev-dependencies]
criterion.workspace = true
tempfile.workspace = true
# Columnar-chain benchmark (#324 D) — Parquet IO to model the parquet→sql→parquet
# round-trip the Value-tax measurement attributes against. Bench-only.
parquet.workspace = true
bytes.workspace = true
# Columnar pipeline acceptance test (#375): drive a Pipeline over a columnar
# source → sql → columnar sink and assert the columnar path is taken.
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
futures.workspace = true
# HTTP reference-relation integration test (#558): mock the REST endpoint and
# assert it is fetched exactly once across multiple pages.
wiremock = "0.6"

[[bench]]
name = "sql_transform"
harness = false

[[bench]]
name = "columnar_roundtrip"
harness = false

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

# API-stability (cargo-semver-checks) lint overrides.
#
# Additive evolution is a MINOR bump by declared contract (CLAUDE.md
# "API-Evolution & Versioning Policy"): struct-literal construction and
# exhaustive matching of config/data types are NOT part of this crate's
# stability surface — construct via serde deserialization, `new()`/`Default`,
# or builders, and match with a `_` arm. So adding an optional config field
# never forces a major. Every other API-stability lint stays at its default
# (deny) level, so genuine removals and signature changes still fail the gate.
[package.metadata.cargo-semver-checks.lints]
constructible_struct_adds_field = "allow"
constructible_struct_adds_private_field = "allow"
enum_struct_variant_field_added = "allow"
